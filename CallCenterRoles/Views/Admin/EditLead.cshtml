@model IEnumerable<CallCenterRoles.Models.MyLeadModel>
@{
    ViewBag.Title = "EditLead";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="right_col" role="main">
    <!-- top tiles -->
    <div class="page-title">
        <div class="title_left">
            <h3>Lead</h3>
        </div>
    </div>
    <div class="clearfix"></div>
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>Assign Lead</h2>
                    <ul class="nav navbar-right panel_toolbox">
                        <li>
                            <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                        </li>

                    </ul>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <br />
                   
                    @*@using (Html.BeginForm("UpdateLead", "Admin", FormMethod.Post, new { @class = "form-horizontal form-label-left" }))
                    {*@
                    <form id="demo-form2" data-parsley-validate class="form-horizontal form-label-left" method="post" action="~/Admin/UpdateLead">
                        
                        @foreach (var item in Model)
                        {
                            <div class="form-group">
                                <label class="control-label col-md-3 col-sm-3 col-xs-12">
                                    @item.dataFieldName
                                </label>
                                <div class="col-md-6 col-sm-6 col-xs-12">
                                    <div class="col-md-6 col-sm-6 col-xs-12">
                                        <input value="@item.dataFieldValue" name="@item.dataFieldName" type="text"  required="required" class="form-control col-md-7 col-xs-12">
                                    </div>
                                </div>
                            </div>
                            }
                        <input type="hidden" name="LeadId" value="@ViewBag.LeadId" />
                        <div class="ln_solid"></div>
                        <div class="form-group">
                            <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                                <input type="submit" class="btn btn-primary" value="Update" />
                                
                            </div>
                        </div>
                    @*}*@
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- /top tiles -->
</div>

